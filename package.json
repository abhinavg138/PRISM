{
  "name": "prism",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "start": "python -m uvicorn backend.main:app --port 8000",
    "dev": "python -m uvicorn backend.main:app --port 8000 --reload",
    "test": "pytest backend/tests -v"
  }
}
