# Legacy ML Reference Artifacts

> [!NOTE]
> The scripts and models in this directory represent initial exploratory machine learning experiments (XGBoost / TreeSHAP) developed during early prototyping.
> 
> The active production PRISM system utilizes the deterministic, auditable **PRISM Risk Engine** (`server/riskEngine.ts`) and **Priority Intervention Engine** (`server/priorityEngine.ts`) grounded directly on MoSPI PAIMANA monthly observations.
> 
> These files are retained solely as offline historical reference.
